---
title: UPI qr code
description: How Dynamic UPI formats QR codes and deep links for payment apps.
sidebar:
  order: 1
---

# UPI QR Format

This page explains the structure behind the generated QR code and intent link.

## QR Data Format

The app uses the following fields when generating a QR code:

- `payeeVPA` — the UPI ID of the receiver.
- `payeeName` — the registered name of the receiver.
- `amount` — payment ammount.
- `currency` — default 'INR'.
- `transactionNote` — optional note shown in the payment app.
- `transactionId` — a unique ID this has to be generated by the app who willbe implementing this .
